Hi,

Open a Terminal and type in

defaults write com.apple.finder AppleShowAllFiles true

then restart your Finder (i.e. log out or force-restart it using Alt-Option-Esc). The hidden files are now shown. To undo this just use false at the end of the command and repeat.
